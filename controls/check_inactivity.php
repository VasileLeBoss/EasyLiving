<?php 
// $inactiveTimeout = 7200; // 2h 
// if (Utilisateur::estConnecte()) {
    
//     if (isset($_SESSION['lastActivity']) && (time() - $_SESSION['lastActivity'] > $inactiveTimeout)) {
//         session_unset();
//         session_destroy();
//         header('Location: v_accueil.php');
//         exit;
//     }
//     $_SESSION['lastActivity'] = time();
// } else {

// }
?>